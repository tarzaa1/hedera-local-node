apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: haveged-1
spec:
  replicas: 1
  serviceName: "haveged"
  selector:
    matchLabels:
      app: haveged
  template:
    metadata:
      labels:
        app: haveged
    spec:
      nodeName: clever-shradda-work02
      containers:
      - name: haveged-1
        image: gcr.io/hedera-registry/network-node-haveged:0.46.3
        args: ["-d 16"]
        resources:
          limits:
            memory: "64Mi"
          requests:
            memory: "32Mi"
        securityContext:
          privileged: true

